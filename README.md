# voice_tren
# 🦁 RT Sales Trainer (MVP)

Telegram Mini App (Web App) для тренировки навыков продаж и отработки возражений в стилистике **Ростелекома**. 

Приложение геймифицирует процесс обучения: сотрудники слушают ответы коллег, голосуют за лучшие и записывают свои варианты решения типичных кейсов.

![Status](https://img.shields.io/badge/Status-MVP-success)![Tech](https://img.shields.io/badge/Tech-Yandex_Cloud_Functions_+_Supabase-blue)

## 🎮 Функционал

1.  **Судейство (Battle Mode):** Пользователю предлагается пара аудио-ответов других участников на одну тему. Нужно выбрать лучший.
2.  **Запись (Recording Mode):** Пользователь получает случайное возражение (например, "Дорого", "Уже есть интернет") и должен записать голосовой ответ.
3.  **Рейтинг (Leaderboard):** Топ сотрудников с лучшими ответами по результатам голосования.

## 🛠 Технологический стек

*   **Frontend:** HTML5, Vanilla JS, TailwindCSS (через CDN). Работает как Telegram Web App.
*   **Backend:** Yandex Cloud Functions (Python 3.x).
*   **База данных & Хранилище:** Supabase (PostgreSQL + Storage).

## 🚀 Установка и запуск

### 1. База данных (Supabase)

Проект использует **Supabase**. Необходимо создать таблицу и бакет для файлов.

**SQL Schema:**
Выполните этот SQL запрос в редакторе Supabase:

```sql
create table records (
  id uuid default gen_random_uuid() primary key,
  created_at timestamp with time zone default timezone('utc'::text, now()) not null,
  user_id bigint,
  first_name text,
  username text,
  topic text,
  file_url text,
  votes int default 0
);
